# The repo carries the SITE ONLY: index.html, the preprint PDF, and the assets
# index.html actually loads. Everything below either lives elsewhere (the decks
# are in OneDrive), is a build input rather than an output, or is no longer
# referenced by the page.
#
# To include something again, delete its line and `git add` it.

# --- source decks, backups and previews (41 MB) ---------------------------
animation/

# --- the emitter-motion generator, its frames and 11 GIF variants (31 MB) --
# The 132 frames the page actually plays are committed, under
# assets/concept2/gen_frames/.
generate_pilpel_motion/

# --- read-only clone of the template this page was built from -------------
_reference/

# --- scratch pages: early concept mock-ups, the font comparison, backups ---
_*.html

# --- project notes, kept out on request ("site only") ---------------------
# NOTE: HANDOFF.md is the only record of how the figures were built and of the
# Office-automation traps behind them. It is NOT backed up anywhere else.
HANDOFF.md
README.md

# --- assets the page no longer references ---------------------------------
assets/concept/
assets/figures/
assets/results_components.png

# unbrightened sources of the two *_bright.png panels the page uses, and the
# global-BG-with-noise variant kept for a possible three-card fan
assets/concept2/concept_globalbgandnoise.png
assets/concept2/concept_input.png
assets/concept2/concept_localbg.png
assets/concept2/concept_noise.png

# the page hard-codes frame count and fps, so it never loads this
assets/concept2/gen_frames/manifest.json

# --- OS / editor noise ----------------------------------------------------
Thumbs.db
desktop.ini
.DS_Store
~$*
